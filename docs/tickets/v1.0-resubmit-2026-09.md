# v1.0 resubmit — after Apple's 2026-06-09 rejection

**Status:** 🔧 Code fixes done and verified. Blocked on App Store Connect edits + upload auth.
**Rejection date:** 2026-06-09 · **Submission ID:** `b0e1fc3a-dc6d-4bbc-b5d7-6bb34f6de01f`
**Reviewed:** 1.0 (1) on a MacBook Pro (14-inch, Nov 2024)
**Resubmitting as:** 1.0.0 build **4** (same marketing version — the 1.0 page is still open)

---

## What Apple actually said, and what we did

### ① Guideline 2.4.5(vii) — "The app updates itself outside of the Mac App Store"

They attached a screenshot of the menu with **"Check for Updates…"** highlighted.

**Root cause.** The Review Notes we submitted claimed the sandboxed build skipped
the update check. That was not true of the code. The only guard was runtime —
`UpdateChecker.check()` returned early when `isAppStoreInstalled()`, which tests
for an App Store *receipt*. **App Review installs from the `.pkg`, which has no
receipt**, so the guard evaluated false on the reviewer's machine. The menu item
itself was never behind even that check.

**Fix (commit `3668bb0`).** Compile gate, not a runtime test. The whole
`UpdateChecker` unit, the launch-time check, the "Update available" submenu, the
"Check for Updates…" item, its actions, and the GitHub hosts in the
`isSafeOpenURL` allowlist are all `#if !MODELSTATUS_APP_STORE`.

**Proof — against the binary, not the source:**

| string | direct build | App Store build |
|---|---|---|
| `Check for Updates` | 1 | 0 |
| `Update available` | 1 | 0 |
| `api.github.com` | 2 | 0 |
| `releases/latest` | 1 | 0 |
| `brew upgrade --cask` | 2 | 0 |
| `githubusercontent` | 1 | 0 |

The Settings "GitHub" button is also hidden in the App Store build (commit
`64f06d0`) — that repo's Releases page serves a downloadable `.app`.

### ② Guideline 2.1 — App Completeness (delivered as a hardware demo-video request)

Their screenshot shows the entire app: one row, **"Local — Unreachable"**.

**Root cause.** `AppConfig.default` hard-coded an instance at Ollama's default
port. On a machine without Ollama that could only ever render as a red ✗, with
nothing explaining it and no next step.

**Fix (commit `64f06d0`).** Nothing is seeded. First launch probes **loopback
only** (`Discovery.probeLoopback`) and adopts whatever is actually running,
named "<Kind> on this Mac". If nothing answers, the menu says "No AI server
found yet", names the server kinds it looked for, and offers "Add a Server…"
and "How do I set this up?".

Loopback deliberately, not `Discovery.scan()`: a /24 sweep trips the macOS
local-network permission prompt and reaches other machines, so it stays behind
an explicit user action.

**Proof — verified by running the App Store `.app`:** from an empty config,
0 instances before launch → 1 after, `"Ollama on this Mac"`.

⏳ **Still owed:** a demo-video URL in App Review Information, plus a reply
correcting the hardware premise (see the draft below).

### ③ Guideline 5 — Legal (China deep-synthesis permitting)

> "the app's metadata includes the following references to ChatGPT and/or OpenAI: OpenAI"

**Decision: deselect China mainland** in Pricing and Availability. Apple asked
for two things — remove the metadata references *and* deactivate the
functionality in the China build. App Store Connect ships one binary to every
storefront, so "deactivate in China" would mean removing OpenAI-compatible
endpoint support for everyone. Deselecting China removes nothing and
misrepresents nothing.

⏳ **Still owed:** the availability change in App Store Connect.

---

## Verified, and it corrected a documented assumption

### `.local` hostnames DO work under the App Store sandbox

The June ticket concluded "`.local` users on the App Store build enter IPv4
directly", on the reasoning that the C1 fix removed `dscacheutil`. That
conclusion was wrong, and it mattered: it implied every App Store user with a
second Mac would have to find and type raw IP addresses.

Tested on the **signed, sandboxed** `.app` (Apple Distribution +
`ModelStatus-AppStore.entitlements`), with controls:

| instance | result |
|---|---|
| CONTROL loopback `127.0.0.1:11434` | idle · 5 available · 9ms |
| CONTROL raw LAN IP `192.168.1.50:11434` | idle · 2 available · 33ms |
| TEST mDNS `macmini-m4-pro.local:11434` | **idle · 2 available · 45ms** |

Proof the sandbox was actually enforced rather than merely signed: the binary
read its config from
`~/Library/Containers/com.lucasmullikin.ModelStatus/Data/Library/Preferences/`
instead of `~/Library/Preferences/`. That redirect only happens inside the
sandbox — an unsandboxed run would have picked up the other config and printed
different server names.

`dscacheutil` was only a cache/diagnostic path. URLSession resolves `.local`
through mDNSResponder via ordinary DNS APIs, which `com.apple.security.network.client`
permits. No entitlement change needed and no setup cliff.

### Upload auth is wired for both paths

`scripts/package-appstore.sh` now prefers an App Store Connect API key
(`--apiKey`/`--apiIssuer`), deriving the key id from
`~/.appstoreconnect/private_keys/AuthKey_<KEYID>.p8` and reading the issuer from
`ASC_ISSUER_ID` or `~/.appstoreconnect/issuer_id`. It falls back to
`ASC_APPLE_ID`/`ASC_APP_PASSWORD`. Verified against four cases: no credentials
(clear error naming both paths), one key (resolves), **two keys with no
`ASC_KEY_ID` (refuses rather than picking one silently)**, and password-only.

---

## Remaining checklist

- [ ] App Store Connect → Pricing and Availability → deselect **China mainland**
- [ ] Generate ASC API key (Developer role) → `~/.appstoreconnect/private_keys/`
- [ ] Wire `--apiKey`/`--apiIssuer` into `scripts/package-appstore.sh`
- [ ] Record the demo video, host it, put the URL in App Review Information
- [ ] Recapture the 5 screenshots — the current set shows **v0.2.1** and the GitHub button
- [x] `CFBundleVersion` 3 → 4
- [ ] Replace the Review Notes (draft below)
- [ ] Build, sign, validate, upload build 4
- [ ] Select build 4 on the 1.0 page → Submit for Review

---

## Session close 2026-09-11 — exact resume point

**The code is finished.** `build/ModelStatus-v1.0.0-4.pkg` (2.4 MB) is built and
signed — Apple Distribution inside, 3rd Party Mac Developer Installer outside,
hardened runtime, 1.0.0 build 4 — and verified free of every string Apple
rejected us for. Nothing further to write.

Three blockers remain, none of them code:

1. **No upload credential.** The ASC API key was never downloaded (searched
   `~/Downloads`, Desktop, Documents, /tmp + Spotlight for `AuthKey`; nothing).
   If a key row exists under Active but was never downloaded it is permanently
   unusable — revoke it before generating another, or `package-appstore.sh`
   will find two candidates and refuse to choose (deliberate).
   **Fastest route around it: Transporter.app, or an app-specific password
   minted while signed in as `lucasstuff@protonmail.com`.**
2. ⚠️ **Screen recording is TCC-blocked for this session.** `screencapture -v`
   hangs indefinitely, writes no file, and surfaces no dialog the agent can
   answer. Granting Screen Recording requires relaunching the terminal, which
   kills the Claude session — so **the demo video must be recorded by the
   operator with Cmd+Shift+5.** Shot list is in the session transcript;
   6 beats, ~75s, the empty-state frame is the one that matters.
3. **App Store Connect edits are unstarted** — the browser MCP (`opentabs`)
   died with HTTP 429 mid-session, and ASC sessions expire fast (dropped twice).
   Owed there: deselect China mainland, replace the Review Notes, attach new
   screenshots, add the demo URL, select build 4, post the Resolution Center
   reply.

**Do not post the drafted Resolution Center reply until the video exists** — it
states "A demo video is linked in App Review Information". Telling App Review
something untrue about the submission is the exact mistake that caused this
rejection.

**Never assessed:** the `/24` LAN scan in `Discovery.scan()` against Guideline
2.5.1, specifically what the user sees if they DENY the local-network prompt.
Both Fable agents tasked with it died without reporting. This is the most
plausible next rejection reason.

---

## Draft — new Review Notes

> ModelStatus is a menu bar utility that monitors AI model servers over HTTP.
> It is software-only: there is no hardware, no device pairing, no companion
> accessory, and no sign-in.
>
> Exercising the app during review:
>
> 1. Launch from /Applications. A brain icon appears in the menu bar.
> 2. With no model server installed, the menu reads "No AI server found yet"
>    and offers "Add a Server…" and "How do I set this up?". This is the
>    intended empty state, not a failure.
> 3. To see it monitoring something, install any local model server (Ollama
>    from ollama.com is the quickest) and relaunch. The app probes this Mac on
>    launch and adds what it finds automatically.
> 4. Alternatively, Settings → Add and enter any HTTP URL. Servers that expose
>    only a /health endpoint work via the "HTTP Health" kind.
>
> Network activity, in full:
> - HTTP polling of the server URLs the user configures.
> - An on-demand local-network scan, only when the user clicks "Discover
>   Servers…" in Settings. Never automatic.
>
> This build performs no update checks of any kind. Following the 2026-06-09
> rejection under Guideline 2.4.5(vii), the entire updater is removed from the
> App Store build at compile time — the binary contains no update UI, no
> release endpoint, and no upgrade instructions.
>
> No telemetry, no analytics, no accounts, no external backend.

## Draft — reply to App Review

> Thank you for the review.
>
> Guideline 2.4.5(vii): fixed. The "Check for Updates…" item you screenshotted
> is gone. Our previous notes said the sandboxed build skipped the update check
> — that was incorrect, and we apologise. The guard was a runtime App Store
> receipt test, which is false when the app is installed from the .pkg during
> review. The entire updater is now excluded from the App Store build at
> compile time; the binary contains no update UI and no release endpoint.
>
> Guideline 2.1: we think the demo-video request may have been issued from a
> hardware-pairing template. ModelStatus does not pair with any hardware — it
> monitors software HTTP endpoints on the user's own machine or network. We
> have also fixed the underlying problem your screenshot shows: the app
> previously shipped a hard-coded server entry that appeared as "Unreachable"
> on any machine without a model server installed. It now detects what is
> running and, when nothing is, explains that and offers next steps. A demo
> video is linked in App Review Information.
>
> Guideline 5: we have removed China mainland from the app's availability.
